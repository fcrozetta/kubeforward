version: 1
metadata:
  project: invalid-production-inherited-detach
defaults:
  namespace: default
environments:
  base:
    forwards:
      - name: api
        resource:
          kind: deployment
          name: api
        ports:
          - local: 7000
            remote: 80
  prod:
    extends: base
    guards:
      allowProduction: true
